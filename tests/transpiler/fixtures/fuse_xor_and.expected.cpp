// AUTO-GENERATED by sturm-transpile -- do not edit
// Source: fuse_xor_and.cpp
// Phase J / PJ-1g input for the sturm-transpile snapshot test.
//
// Canonical zero-ancilla fusion pair: a `qbool __t = a & b;` VarDecl
// whose init is a bare `&` op-call with two DeclRefExpr operands,
// immediately followed by an `x ^= __t;` compound-assign whose RHS is
// exactly that temporary. `__t` has exactly one reader in the enclosing
// scope (the `^=` RHS itself), so the PJ-1d peephole matcher fuses
// the pair into a single `ccnot_inplace(x, a, b);` call; the QReplacement
// spans both statements. The M8 uncompute pass then injects a second
// `ccnot_inplace(x, a, b);` at scope close — CCX is self-adjoint
// per the PJ-1c render case.
//
// The stub below mirrors the one used by the PJ-1d unit tests in
// `transpiler/tests/test_matcher_ccnot_fuse.cpp` — hermetic, no include
// paths required. The sturm-transpile binary runs with a
// FixedCompilationDatabase that carries no include paths, so the file
// is self-contained; only `operator&` and `operator^=` need to resolve
// for the matcher's AST anchor to fire.
namespace sturm {
class qbool {
public:
    qbool() {}
    qbool(const qbool&) {}
    qbool& operator=(const qbool&) { return *this; }
    qbool& operator^=(const qbool&) { return *this; }
};
inline qbool operator&(const qbool&, const qbool&) { return qbool{}; }
} // namespace sturm
using sturm::qbool;

void demo(qbool a, qbool b, qbool x) {
    ccnot_inplace(x, a, b);
    ccnot_inplace(x, a, b);
}
